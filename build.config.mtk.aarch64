export ARCH=arm64
export CROSS_COMPILE=aarch64-linux-gnu-
export LLVM=1
export LLVM_IAS=1
# DEFCONFIG=defconfig
export CLANG_PREBUILT_BIN=prebuilts/clang/host/linux-x86/clang-r383902/bin
# for check scripts
export CC=clang
export LD=ld.lld
export AR=llvm-ar
export NM=llvm-nm
export OBJCOPY=llvm-objcopy
export OBJDUMP=llvm-objdump
export READELF=llvm-readelf
export OBJSIZE=llvm-size
export STRIP=llvm-strip
#export PATH=$CLANG_PREBUILT_BIN:$PATH
